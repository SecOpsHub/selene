---
# Selene Ansible Collection Requirements
# Installed by UserData before running site.yml:
#   ansible-galaxy collection install -r ansible/requirements.yml
 
collections:
  - name: amazon.aws
    version: ">=6.0.0,<9.0.0"
 
