---
# Role: wazuh_indexer
# Purpose: Configure Wazuh to forward alerts to external Amazon OpenSearch Service
# Spec: docs/components/SPEC-006-ansible.md, SPEC-004-opensearch.md
#
# Status: PLACEHOLDER — implementation pending Phase 4

# Tasks will:
# 1. Configure /etc/wazuh-indexer/opensearch.yml to point at opensearch_endpoint
# 2. Configure Wazuh manager output to use external OpenSearch (not local indexer)
# 3. Notify wazuh_services handler if config changed
