---
# Role: wazuh_rules
# Purpose: Deploy custom rules and suppressions from Git to /var/ossec/etc/
# Spec: docs/components/SPEC-006-ansible.md
#
# Status: PLACEHOLDER — implementation pending Phase 4

# Tasks will:
# 1. Copy wazuh/rules/local_rules.xml → /var/ossec/etc/rules/local_rules.xml
# 2. Copy wazuh/suppressions/local_overrides.xml → /var/ossec/etc/local_overrides.xml
# 3. Notify wazuh_services handler if either file changed
